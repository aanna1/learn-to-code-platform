/* Starter the learner is given. The loop body is missing, so it returns 0 for every n. */
#include <stdio.h>

int sum_to(int n) {
    int total = 0;
    /* TODO: add up the numbers from 1 to n */
    return total;
}

int main(void) {
    printf("sum_to(10) = %d\n", sum_to(10));
    return 0;
}
