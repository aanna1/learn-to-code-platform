def classify_triangle(a, b, c):
    # TODO:
    # 1. Validity first. Every side must be positive AND each pair of sides
    #    must sum to more than the third (the triangle inequality).
    #    If invalid, return "invalid".
    # 2. Otherwise classify: all three equal -> "equilateral",
    #    exactly two equal -> "isosceles", none equal -> "scalene".
    pass
