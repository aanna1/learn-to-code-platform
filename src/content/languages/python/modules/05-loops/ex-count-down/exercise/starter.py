def count_down(n):
    # Write your while loop here.
    # Hints:
    #   1. n is already your starting value — no separate variable needed
    #   2. The condition: keep looping while n is still >= 1
    #   3. Inside the loop: print n, then decrease it by 1 (n -= 1)
    pass


if __name__ == "__main__":
    count_down(5)
