-- This only computes the headcount.
-- TODO: also compute the total payroll, average salary, lowest salary, and
-- highest salary -- SUM, AVG, MIN, MAX -- each aliased the way headcount is.
SELECT COUNT(*) AS headcount FROM employee;
