`GRANT`, `REVOKE`, and roles are the mechanism that turns Module 11's "least privilege" from a slogan into enforced policy — but they don't run in the browser's SQLite, which has no user accounts at all. They're Oracle/server concepts you learn cold for the handwritten exam, so this exercise is pure reasoning.

Work through the four questions: which sublanguage `GRANT`/`REVOKE` are, why precise grants beat `GRANT ALL`, what a role-level `REVOKE` does to every member, and why `GRANT` is a *syntax error* in the course IDE. Use the **draft** box to think it through first (it isn't graded); then complete the fields and press **Submit answer**.

<Callout type="note">
This is a **Track-2 reasoning exercise** — there's no SQL to run, because the browser engine has no `GRANT`/`REVOKE`. That dialect gap is itself one of the graded questions.
</Callout>
