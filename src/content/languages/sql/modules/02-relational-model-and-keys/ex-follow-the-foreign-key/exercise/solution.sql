-- branch_id is the foreign key: each value is a branch's primary key. With it
-- on the grid, you can follow Michael's 2 to the branch table's row 2, Scranton.
SELECT first_name, branch_id FROM employee;
