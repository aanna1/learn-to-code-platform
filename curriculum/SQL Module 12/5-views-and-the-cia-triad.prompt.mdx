You just *built* views in the IDE. This exercise is the reasoning around them: why a view is a real security boundary (not a UI trick), what auditing adds on top of access control, and how the whole module's controls map back to the **CIA triad** from Module 01.

Answer the four questions: the right way to hide salaries from a team lead, why a view counts as an access-control boundary, what an audit log gives you beyond "who can act," and which part of the CIA triad each control protects. Use the **draft** box to lay out the triad first (it isn't graded); then complete the fields and press **Submit answer**.

<Callout type="note">
Confidentiality (kept private), Integrity (correct and accountable), Availability (still there when needed) — access control and views serve the first, auditing the second, backup and recovery the third.
</Callout>
