-- This query returns only each employee's name — you can't tell which branch
-- anyone works in.
-- TODO: also return the foreign key column that links each employee to a
-- branch. Return first_name and branch_id from the employee table.
SELECT first_name FROM employee;
