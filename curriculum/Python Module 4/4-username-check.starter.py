def check_username(name):
    # TODO: Check the rules in order and return the first failing message:
    #   1. empty            -> "Username cannot be empty."
    #   2. shorter than 3   -> "Username is too short."
    #   3. contains a space -> "Username cannot contain spaces."
    #   otherwise           -> "Username is valid."
    # Use an if/elif/else chain so only the first failure is reported.
    pass
