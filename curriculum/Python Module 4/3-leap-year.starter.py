def is_leap_year(year):
    # TODO: Return True if year is a leap year, False otherwise.
    # Rule: divisible by 4, but not by 100 — unless also divisible by 400.
    # Use % to test divisibility (year % n == 0) and combine with and/or/not.
    pass
