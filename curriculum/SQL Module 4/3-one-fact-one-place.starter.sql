-- This shows the branch ids but not the fact that matters — the city. To see
-- that each city is stored exactly once, you need the city column on the grid.
-- TODO: return branch_id and city (in that order) from the branch table.
SELECT branch_id FROM branch;
