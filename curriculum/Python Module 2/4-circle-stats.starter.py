import math


def circle_stats(radius):
    """Return (area, circumference) for a circle, both rounded to 2 decimal places."""
    # TODO: compute area using math.pi and **
    # TODO: compute circumference using math.pi
    # TODO: return a tuple (area, circumference), both rounded to 2 decimal places
    pass


if __name__ == "__main__":
    print(circle_stats(5))   # should print (78.54, 31.42)
    print(circle_stats(1))   # should print (3.14, 6.28)
